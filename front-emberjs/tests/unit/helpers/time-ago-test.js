import { timeAgo } from 'it-package/helpers/time-ago';
import { module, test } from 'qunit';

module('Unit | Helper | time ago');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeAgo([42]);
  assert.ok(result);
});
