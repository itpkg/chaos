{{#if alertBox.item}}
<div class="alert alert-{{alertBox.item.style}} alert-dismissible" role="alert">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <strong>{{alertBox.item.created}}：</strong>
    <ol>
    {{#each alertBox.item.messages as |m|}}
        <li>{{m}}</li>
    {{/each}}
    </ol>
</div>
{{/if}}
