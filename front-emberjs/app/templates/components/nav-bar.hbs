<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">{{siteInfo.item.title}}</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            {{#link-to 'index' class="navbar-brand"}} {{siteInfo.item.subTitle}} {{/link-to}}
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                {{#each siteInfo.item.links as |l|}}
                    <li>{{#link-to l.href}} {{l.label}} {{/link-to}}</li>
                {{/each}}
            </ul>
            <ul class="nav navbar-nav navbar-right">
                {{#each i18n.locales as |l|}}
                    <li><a {{action "switchLang" l}}>{{t (concat "lang." l)}}</a></li>
                {{/each}}
            </ul>
        </div>
    </div>
</nav>
